<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 64 64">
  <!-- Teal rounded background -->
  <rect width="64" height="64" rx="14" fill="#0D9488"/>
  <!-- Bold M — clear at any size -->
  <text
    x="32" y="47"
    font-family="Inter, Arial Black, sans-serif"
    font-size="44"
    font-weight="900"
    fill="white"
    text-anchor="middle"
    letter-spacing="-2"
  >M</text>
  <!-- Small orange dot accent (brand accent color) -->
  <circle cx="50" cy="14" r="7" fill="#F59E0B"/>
</svg>
